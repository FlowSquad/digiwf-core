id: task-management
author: Simon Zambrovski
realm: ${SSO_REALM}
changes:
  - addSimpleClient:
      clientId: ${SSO_TASK_CLIENT_ID}
      secret: ${SSO_TASK_CLIENT_SECRET}
      publicClient: false
      redirectUris:
        - ${HOST_URL}:${TASKSERVICE_SERVER_PORT}/*
